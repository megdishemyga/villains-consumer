spring:
  data:
    mongodb:
      database: ${database.name}
      uri: ${database.protocol}${database.username}${database.user_password_seperator}${database.password}${database.host_seperator}${database.host}/${database.auth_db}${database.params_seperator}${database.cnx_params}
      authentication-database:
      auto-index-creation: true
  kafka:
    consumer:
      group-id: bat-cave
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: com.gotham.villains.infra.kafka.inbound.util.VillainAvroTestDeserializer
    producer:
      client-id: bat-cave
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: com.gotham.villains.infra.kafka.inbound.util.VillainAvroTestSerializer

database:
  protocol: 'mongodb://'
  host: localhost:27017
  name: BAT-CAVE
  username: myga-dev
  password: myga-dev
  auth_db: admin
  params_seperator:
  user_password_seperator: ':'
  seperator_host_port: ':'
  host_seperator: '@'
  cnx_params: